[package]
name = "multicore-simulator"
version = "0.1.0"
edition = "2021"
description = "Rust-based multicore execution simulator with cache contention and memory latency modeling"

[dependencies]

[dev-dependencies]

[profile.release]
lto = true
codegen-units = 1
